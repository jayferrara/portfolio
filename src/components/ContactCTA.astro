<section class="cta-module">
  <div class="container">
    <h2 class="cta-title">There's more behind this work.</h2>
    <p class="cta-body">What's shown here is the visible outcome. The real impact came from aligning teams, simplifying complexity, and making hard prioritization calls under pressure.</p>
    <p class="cta-body">If you're exploring what strong product design leadership looks like in practice, I'd welcome the conversation.</p>
    <a href="mailto:jsferrara@gmail.com" class="cta-button">Drop Me a Line</a>
  </div>
</section>

<style>
  .cta-module {
    margin-top: 2rem;
    padding: 4rem 0;
    border-top: 1px solid var(--color-border);
    text-align: center;
  }

  .cta-title {
    font-size: clamp(1.75rem, 3.5vw, 2.25rem);
    font-weight: 500;
    letter-spacing: -0.03em;
    line-height: 1.15;
    margin-bottom: 1.25rem;
  }

  .cta-body {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    max-width: 36rem;
    margin: 0 auto 0.75rem;
  }

  .cta-body:last-of-type {
    margin-bottom: 2rem;
  }

  .cta-button {
    display: inline-block;
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    color: var(--color-bg);
    background: var(--color-text);
    padding: 0.75rem 1.75rem;
    border-radius: 6px;
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .cta-button:hover {
    opacity: 0.8;
  }
</style>
